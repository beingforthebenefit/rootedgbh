---
const steps = [
  {
    title: "Initial Assessment",
    description: "We conduct a comprehensive assessment to understand your child's unique needs and develop a personalized plan."
  },
  {
    title: "Treatment Implementation",
    description: "We implement a tailored program using evidence-based techniques and strategies to help your child succeed."
  },
  {
    title: "Goal Setting",
    description: "We work with you to establish clear, achievable goals that address your child's specific challenges and promote their growth."
  },
  {
    title: "Regular Monitoring & Adjustments",
    description: "We regularly monitor progress and make adjustments to the treatment plan as needed to ensure ongoing success."
  }
];
---

<section class="py-20 bg-gray-50">
  <div class="container mx-auto px-6">
    <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Individualized Treatment Plans</h2>
    
    <!-- Mobile View -->
    <div class="md:hidden space-y-8">
      {steps.map((step, index) => (
        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="flex items-start gap-4">
            <span class="text-2xl font-bold text-purple-700">{index + 1}</span>
            <div>
              <h3 class="text-xl font-bold mb-2">{step.title}</h3>
              <p class="text-gray-600">{step.description}</p>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Desktop View -->
    <div class="hidden md:grid grid-cols-3 gap-8 max-w-6xl mx-auto">
      <div class="space-y-32 text-right">
        <div>
          <h3 class="text-xl font-bold mb-2">{steps[0].title}</h3>
          <p class="text-gray-600">{steps[0].description}</p>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-2">{steps[2].title}</h3>
          <p class="text-gray-600">{steps[2].description}</p>
        </div>
      </div>
      
      <div class="flex flex-col items-center space-y-32 text-4xl font-bold text-purple-700">
        <span>1</span>
        <span>2</span>
        <span>3</span>
        <span>4</span>
      </div>

      <div class="space-y-32">
        <div>
          <h3 class="text-xl font-bold mb-2">{steps[1].title}</h3>
          <p class="text-gray-600">{steps[1].description}</p>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-2">{steps[3].title}</h3>
          <p class="text-gray-600">{steps[3].description}</p>
        </div>
      </div>
    </div>
  </div>
</section>